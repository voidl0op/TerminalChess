#include <stdbool.h>
#include "types.h"

bool is_valid(pos dest) {
  if() {}
  if() {}
  if() {}
  if() {}
  return true;
}

bool is_legal(int pce,pos from, pos dest) {
  switch (pce){
    case EMPTY:
      return false;
    case PAWN:
      if() {}
      else return false;
    case BISHOP:
      if() {}
      else return false;
    case KNIGHT:
      if() {}
      else return false;
    case ROOK:
      if() {}
      else return false;
    case QUEEN:
      if () {}
      else return flase;
    default:
      return false;
  }
}
