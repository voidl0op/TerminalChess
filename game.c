#include "board.h"
#include "input.h"
#include "move.h"
#include "rules.h"
#include "types.h"

int main() {
  int grid([8][8]);
  while(1!=0) {
    init(grid[8][8]);
    print(grid[8][8]);
  }
  return 0;
}
